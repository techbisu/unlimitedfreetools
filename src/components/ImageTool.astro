---
import ImageTool from "./tools/ImageTool.jsx";

interface Props {
  initialMode?: "compress" | "convert";
}

const { initialMode = "compress" } = Astro.props;
---

<ImageTool client:load initialMode={initialMode} />
